# Phase 3 — Student UI Dependencies
# Install alongside the main ADK environment:
#   pip install -r requirements-ui.txt
#
# These are ADDITIVE to google-adk — install in the same venv.

streamlit>=1.35.0         # Student-facing web UI
httpx>=0.27.0             # HTTP client for streaming ADK SSE endpoint
uvicorn>=0.29.0           # ASGI server for main.py FastAPI backend
aiosqlite>=0.19.0         # Async SQLite driver (local dev session storage)

# Production (AlloyDB / PostgreSQL session storage) — install when deploying:
# asyncpg>=0.29.0
# greenlet>=3.0.0         # Required by SQLAlchemy async on some platforms
